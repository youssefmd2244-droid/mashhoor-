# التعديلات الجديدة — مشهور (Mashhoor)

هذا الملف بيشرح اللي اتضاف، وكله جوه لوحة التحكم (الإعدادات) وتقدر تفعّله أو تلغيه براحتك.

## تبويبات جديدة في الإعدادات
- **المميزات** — فيها 3 مفاتيح تشغيل/إلغاء:
  - رقم الترابيزة/الأوضة (تقدر تغيّر الاسم وتخليه إجباري أو اختياري)
  - تناول في المكان (Dine-in) كخيار تالت جنب التوصيل والاستلام
  - شاشة تأكيد الطلب على الموقع بعد الإرسال
- **الشكاوي** — ضيف أي عدد من أرقام خدمة العملاء/الشكاوي، هتظهر في آخر الموقع (الفوتر) بزرار اتصال وواتساب.
- **طرق الدفع** — بقيت فيها تفعيل/إلغاء لكل طريقة (كاش، إنستاباي، فودافون كاش، اتصالات كاش) + إمكانية إضافة طريقة دفع مخصصة جديدة.
- **العملة** — دلوقتي إعداد في تبويب "عام"، تختار من EGP/SAR/AED/KWD/QAR/OMR/BHD/JOD/EUR/USD أو تكتب عملة مخصصة، وبتتغير في كل الموقع ورسالة الواتساب تلقائي.

## تبويب الباركود
- بقى فيه وضعين: باركود واحد للموقع (زي الأول)، أو **باركود منفصل لكل ترابيزة/أوضة** برقمها — بيولّد أي عدد تحدده، وكل باركود بيفتح الموقع ويقول تلقائي إن الطلب جاي من رقم كام (لازم تفعّل ميزة "رقم الترابيزة" الأول من تبويب المميزات).

## جديد: ظهور صورة الصنف فعليًا جوه رسالة واتساب
اتنفذت. التفاصيل:
- في لوحة التحكم → المحتوى → الصنف، فيه خانة "أو الصق رابط صورة عام (http/https)". لو حطيت فيها رابط صورة حقيقي (من imgbb.com أو postimages.org أو أي استضافة صور مجانية)، الرابط ده بيتبعت جوه رسالة الطلب في واتساب، وواتساب بيعمله معاينة تلقائية (صورة + عنوان) جوه المحادثة.
- لو الصورة مرفوعة من جهازك بس (يعني مخزنة محليًا جوه المتصفح ومفيهاش رابط عام)، الكود بقى يتجاهلها تلقائيًا من رسالة واتساب (بدل ما يبعت رابط تالف أو نص طويل جدًا)، وهتلاقي تنبيه أصفر جنب الصورة في لوحة التحكم يقولك كده وبيوجهك تحط رابط عام بدالها.
- الرفع المحلي للصورة (زي الأول) لسه شغال وبيظهر في الموقع نفسه عادي — الفرق بس إنه مش هيظهر جوه رسالة واتساب لحد ما يبقى ليه رابط عام.

## جديد: التبديل بين Supabase و Firebase بقى كامل الاتجاهين
لحد دلوقتي كان سحب البيانات (pull) شغال مع Supabase بس. دلوقتي بقى شغال مع Firebase برضو، يعني:
- تفعّل Supabase، تستخدم الخطة المجانية لحد ما تقرب تخلص.
- تروح للإعدادات → Storage، تختار Firebase وتحط بياناته، وتضغط "مزامنة الآن".
- هيسحب أي بيانات كانت اتخزنت قبل كده على Firebase (لو كنت استخدمته قبل) ويدمجها مع اللي عندك محليًا — زي بالظبط ما بيحصل مع Supabase.
- **البيانات المحلية (اللي في المتصفح) متتمسحش أبدًا في أي خطوة من الخطوات دي** — التبديل بين الخيارات بيغيّر بس وجهة المزامنة، مش المصدر الأساسي للبيانات.
- لو أي طلب شبكة فشل لأي سبب (نت واقع، بيانات غلط) بيكمل باقي الجداول عادي بدل ما يوقف كل حاجة.
## جديد: تحديث لحظي فعلي بدون ريفريش (Supabase و Firebase) + أفضل حل ممكن مع GitHub
دلوقتي أي تعديل (إضافة/تعديل/حذف صنف، إلخ) بيوصل لكل الزوار والأجهزة التانية تلقائيًا، من غير ريفريش:
- **Supabase**: اتصال لحظي حقيقي (Realtime عبر WebSocket) — التعديل بيظهر في أقل من ثانية.
- **Firebase**: نفس الشيء بالظبط، عبر Firestore `onSnapshot` — لحظي وحقيقي.
- **GitHub**: **مفيش حل لحظي حقيقي، وده مش قصور في الكود — جيت هاب أصلاً مستودع ملفات مش قاعدة بيانات حية ومفيهوش أي آلية تنبيه فوري**. أقصى حاجة ممكنة هي إن الموقع يتفحص كل ١٠ ثواني تقريبًا هل فيه تغيير (وده مجاني وآمن ومش بياخد من حد الطلبات المسموح بيه لأنه بيستخدم فحص "لسه زي ما هو ولا لأ"). يعني التغيير هيظهر خلال ثواني قليلة، مش في نفس اللحظة بالظبط.
- الإعدادات بتوضح دلوقتي تحت كل provider أيه اللي هيحصل فعليًا (⚡ لحظي / ⏱️ فحص كل ١٠ ثواني) عشان محدش يتفاجئ.
- القائمة (Menu) في الموقع بقت تسمع لأي تغيير في البيانات المحلية (سواء من نفس الجهاز أو من مزامنة لحظية جاية من جهاز تاني) وتحدّث نفسها تلقائيًا.

**متطلب تركيب:** لازم تعمل `npm install` قبل أول تشغيل بعد النسخة دي، عشان اتضافت مكتبتين (`@supabase/supabase-js` و `firebase`) لازمين للاتصال اللحظي. مسحت `package-lock.json` القديم عشان يتولّد واحد جديد صح مع المكتبات الجديدة.

## جديد: شكل احترافي 3D + حركات في شاشة إتمام الطلب والإعدادات
- كروت طريقة الدفع وطريقة الاستلام بقى ليها تأثير 3D خفيف (Tilt) عند اللمس/الماوس، وشاشة "إتمام الطلب" بتظهر بحركة انبثاق ثلاثية الأبعاد بدل ما تظهر فجأة.
- أيقونة جمب كل طريقة دفع (💵 🏦 📱 📲...) — قابلة للتغيير من الإعدادات.
- إيموجي سكوتر توصيل 🛵 متحرك جمب كلمة "توصيل".

## جديد: طرق الدفع بقت قابلة للتعديل والحذف بالكامل
كنت لاحظت إن حاجات كتير مينفعش تتحذف أو تتعدل — اللي لقيته فعليًا هو إن الأربعة طرق الدفع الأساسية (كاش/إنستاباي/فودافون كاش/اتصالات كاش) كانت مقفولة من الحذف بتصميم قديم. اتفتحت بالكامل دلوقتي:
- تقدر تعدّل الاسم والأيقونة لأي طريقة دفع، حتى الأساسية.
- تقدر تحذف أي طريقة دفع، حتى الأساسية.
- زرار "استرجاع الطرق الافتراضية" بيرجعهم لو احتجت، من غير ما يمسح أي طريقة مخصصة ضفتها.
- ضفت اسم صاحب الحساب لكل رقم محفظة، ورسالة "ابعت اسكرين الدفع" بقت قابلة للتعديل بالكامل (عربي/إنجليزي) بدل ما تكون ثابتة في الكود.
**⚠️ ملحوظة ترقية:** شكل تخزين طرق الدفع اتغيّر بالكامل عشان يدعم الأيقونة والتعديل الكامل — أرقام/طرق الدفع اللي كانت متسجلة قبل كده في نسخة قديمة هتترجع للإعدادات الافتراضية، وهتحتاج تدخل الأرقام تاني من Settings → طرق الدفع.

## جديد: أحجام الصنف (سمول / ميديم / لارج)
من الإعدادات → المحتوى → الأصناف، فعّل "الصنف ده له أحجام مختلفة" وضيف كل حجم بسعره. الميزة دي:
- تقدر تفعّلها لأي صنف موجود من قبل، مش بس الجديد.
- تقدر تخفيها في أي وقت من نفس المكان.
- بتظهر للعميل كأزرار اختيار قبل ما يضيف للسلة، والسعر بيتغيّر تلقائيًا حسب الحجم المختار، وبيتبعت في رسالة واتساب مع اسم الحجم.

## جديد: رفع الوسائط بالسحب من الجهاز (Drag & Drop)
أي مكان كان فيه بس "حط رابط صورة" بقى فيه دلوقتي: اسحب ملف من جهازك مباشرة، أو اضغط لاختيار ملف، أو الصق رابط عام — التلاتة بيشتغلوا بنفس الخانة. مطبّق دلوقتي على صورة الصنف واللوجو.

## جديد: فكرة إضافية لمشكلة صورة واتساب
زي ما اتوضح قبل كده، رابط واتساب (wa.me) مبيقدرش يرفق ملف حقيقي أبدًا مهما عملنا — دي حدود المنصة نفسها. فكرة إضافية دلوقتي: على الموبايل، لو صورة الصنف موجودة، بيظهر زرار "📤 ابعت الصورة" بيستخدم خاصية المشاركة الأصلية في الجهاز (Share Sheet) عشان يبعت الصورة الحقيقية (مش رابط) لأي حد يختاره العميل بما فيهم واتساب. الفرق إن الزرار ده لازم العميل يختار المحادثة بنفسه (مينفعش نحدد رقم المطعم تلقائي زي رابط الطلب العادي) — فهو إضافة اختيارية جنب زرار "اطلب الآن" العادي، مش بديل عنه. الزرار بيظهر بس على الأجهزة اللي بتدعم الخاصية دي (موبايلات غالبًا).

## جديد: أوضاع العرض (نهاري / راحة العين / دارك / أبيض وأسود)
من الإعدادات → العرض. التطبيق بيتم بفلتر شامل على الموقع كله (بدل إعادة بناء كل لون في كل مكون)، وده اختيار هندسي متعمد عشان نضمن إنه ميكسرش أي تصميم موجود — فهو أسرع وأأمن من إعادة بناء نظام الألوان بالكامل، لكن معناه إن التغيير شامل (فلتر) مش لون مخصص لكل وضع بالتفصيل.

## جديد: قسم النصوص والخطوط
من الإعدادات → النصوص والخطوط: اختار خط مختلف للموقع كله، وعدّل بعض نصوص الموقع (زي جملة الفوتر). سيب أي خانة فاضية عشان يفضل النص الأصلي.
**بصراحة:** غطّيت أهم نص ظاهر بسهولة (جملة الفوتر) كمثال شغال. عنوان الصفحة الرئيسية (Hero) معملتش له نفس الربط لأنه متصمم بحركة ظهور حرف-بحرف معقدة، وربطه بنص متغير كان محتاج إعادة كتابة الحركة دي من الصفر — تفضيل تجنّب المخاطرة دي على حساب اكتمال الميزة 100%. لو عايزها، قولّي وهعملها في خطوة منفصلة مع اختبار كافي.

## إصلاح: علامة الإعدادات كانت بتتداخل مع شريط اللغة
زي ما لاحظت في الصورة، أيقونة الإعدادات كانت زرار عايم منفصل بيتداخل بصريًا مع زرار اللغة في نفس الركن. اتنقلت دلوقتي جوه شريط التنقل نفسه (نفس الصف اللي فيه زرار اللغة وزرار القائمة)، فبقت متراصة معاهم مش فوقهم.
- كمان ضفت زرار بحث 🔍 جمبها في نفس الشريط — بيودّيك لقسم المنيو ويفتح خانة البحث الموجودة جاهزة فيه (تقدر تدور بيها على اسم أي صنف).

## جديد: شخصية Ember المتحركة (3D) بتتجول في الموقع
حطيت الكود اللي بعته كأساس وضفت عليه:
- بتتفاعل مع الاسكرول: كل ما تمرر الصفحة، بتعمل "قفزة تفاجؤ" صغيرة وممكن تتحرك ناحية حاجة جديدة ظهرت على الشاشة.
- بتزور زراير الطلب والدفع تحديدًا بشكل متكرر (مش عشوائي بس) وبتعمل حركة "إمساك/تفاعل" مع الزرار لما توصله، وأحيانًا بتشاور (تأشير باليد) على الكروت اللي بتوصلها.
- باقي الحركات زي ما بعتها: مشي/جري/قفز/شقلبة للقدام/دوران/جلوس/تلويح بالسلام، وكاميرا بتتبعها أفقيًا.
- الأداء: بتشتغل من غير WebGL بتختفي تلقائيًا (متدعمهاش الأجهزة القديمة جدًا)، وبتحترم إعداد "تقليل الحركة" في الجهاز لو المستخدم مفعّله.

## جديد: صور كل أصناف الطلب الجماعي بتتبعت مع رسالة واتساب
لما تعمل طلب جماعي (تضيف أكتر من صنف للسلة وتبعته مرة واحدة)، أي صنف ليه صورة برابط عام (مش رفع محلي) هيتبعت رابط صورته مع باقي تفاصيل الطلب في نفس رسالة واتساب — مش بس صورة صنف واحد زي الأول.

## جديد: شخصية Ember بقت شكلها الحقيقي "Little Flare" بدل الشكل الهندسي المؤقت
- استبدلت الشكل الهندسي (المكوّن من أشكال بسيطة زي الكرة والمخروط) بالموديل ثلاثي الأبعاد الحقيقي اللي بعته (نفس تصميم "Little Flare")، بملف `public/models/little-flare.glb`.
- الموديل مالوش لون أو مفاصل حركة جاهزة، فحطيت له تدرج لوني ناري (أصفر فاتح لأعلى، برتقالي/أحمر غامق لتحت) وعينين وفم بمكانهم الصح فوق الشكل نفسه.
- كل الحركات (مشي/جري/قفز/شقلبة/دوران/جلوس/تلويح/التفاعل مع زراير الطلب والاسكرول) لسه شغالة بالكامل زي الأول — غيّرت بس طريقة تنفيذ المشي/التلويح عشان الموديل ده جسم واحد من غير أذرع/أرجل منفصلة، فبقت الحركة عبارة عن ميل وترنّح وقفزات صغيرة على الجسم كله بدل تحريك الأذرع، وده مناسب أكتر لشكل الشعلة الصغيرة الظريفة.
- لو الموديل تعذر تحميله لأي سبب (مشكلة شبكة مثلًا)، فيه شكل بديل بسيط بيظهر تلقائي عشان الشخصية توجد دايمًا ومتكسرش الموقع.
- حجم الشخصية اتظبط يكون صغير ومش بيضايق أي زرار أو محتوى.

## قريبًا (مرحلة جاية لحجمها)
- خيارات الصنف (سمول/ميديم/لارج + إضافات بسعر إضافي)
- وسم حساسية/نظام غذائي على كل صنف
- سجل طلبات سابقة للعميل لإعادة الطلب بضغطة واحدة
- ورقة مطبخ (Kitchen ticket) منفصلة عن رسالة الواتساب
- استضافة صور الأصناف تلقائيًا من داخل المشروع نفسه (من غير ما تحتاج تستخدم موقع خارجي زي imgbb)

## طريقة التركيب
الملفات هنا بديل مباشر لنفس المسارات في المشروع — انسخهم فوق المشروع الحالي، شغّل `npm install` مرة واحدة (عشان المكتبات الجديدة)، وبعدها هيشتغل عادي.

## جديد: شيل الشخصية (Ember / Little Flare)
اتشالت بالكامل من الموقع — الملف `EmberCompanion.tsx`، الموديل ثلاثي الأبعاد `little-flare.glb`، وكل استدعاء ليها في `App.tsx`. مفيش أي أثر أو كود ميت وراها.

## جديد: رفع فيديوهات حقيقي (مش منظر) عبر Vercel Blob Storage
- من لوحة التحكم → المحتوى → "اسم الموقع واللوجو" فيه دلوقتي حقل "الفيديو التعريفي" بيرفع الفيديو فعليًا لـ Vercel Blob Storage (مش base64 زي الصور، لأن الفيديو كبير قوي على كده) ويحطه يشتغل في أول الموقع (Hero) تلقائي.
- عشان ده يشتغل: لازم المشروع يبقى منشور على Vercel، وتفعّل Blob Storage من تبويب Storage في مشروعك على Vercel (مجاني، خطوة واحدة، Vercel بيولّد الـ token لوحده — مفيش حاجة تحطها يدوي).
- لو حاولت ترفع وهو مش شغال على Vercel، هيقولك بالظبط إيه المطلوب بدل ما يفشل بصمت.
- الملفات الجديدة: `api/upload.ts` (الفنكشن اللي بتولّد توكن رفع آمن)، `vercel.json`، `src/lib/videoUpload.ts`، `src/components/admin/VideoUploadField.tsx`.

## جديد: قسم "الإضافات" (Extras) — تبويب كامل في الإعدادات
- تبويب جديد اسمه **الإضافات** في لوحة التحكم: تضيف إضافة (اسم عربي/إنجليزي + سعر + صورة). الصورة بتتصغّر تلقائيًا لـ 64×64 بيكسل وقت الرفع (مش بس CSS) عشان تفضل شكلها badge صغير مش صورة كبيرة.
- وقت ما العميل بيدخل بياناته قبل إرسال الطلب (في شاشة "إتمام الطلب")، بتظهر له كل الإضافات المتاحة كـ chips يقدر يختار منها أكتر من واحدة في نفس الوقت.
- الإضافات المختارة بتتضاف فعليًا لإجمالي الطلب، وبتتبعت في رسالة الواتساب في قسم منفصل "الإضافات"، وبتتسجل في سجل الطلب (OrderRecord).
- جدول الإضافات (`extras`) بقى جزء من المزامنة السحابية زي أي جدول تاني (Supabase / Firebase / GitHub) — يعني لو غيرت الإضافات من جهاز، هتتزامن مع باقي أجهزتك زي أي بيانات تانية في الموقع.

## جديد: مزامنة سحابية تلقائية مستمرة + تأكد حقيقي من نجاح الحفظ
- كل تعديل بتعمله (منتج، سعر، طلب، إعداد...) بقى **بيتزامن لوحده تلقائيًا** مع أي تخزين سحابي مفعّل (Supabase / Firebase / GitHub) خلال ثوانٍ من غير ما تحتاج تدوس "مزامنة الآن" — الزرار لسه موجود لو حبيت تعمل مزامنة فورية يدوي.
- لو فشلت المحاولة (نت واقع، مشكلة مؤقتة)، بيعيد المحاولة تلقائيًا لحد 5 مرات بفواصل متزايدة، وبيحاول تاني فورًا أول ما جهازك يرجع أونلاين — من غير أي تدخل منك.
- كل طلبات الشبكة للتخزين السحابي بقى ليها **مهلة زمنية (20 ثانية)** عشان اتصال ضعيف مايهنجش الموقع أو شاشة الإعدادات.
- الحفظ على Supabase/Firebase/GitHub بقى **بيتحقق فعليًا من نجاح الطلب** (بدل ما يفترض النجاح لمجرد إن الطلب اتبعت) — لو فيه مشكلة حقيقية (باسورد غلط، صلاحيات...) هتلاقيها في رسالة واضحة بدل "تم بنجاح" وهمية.
- تبويب "التخزين" في الإعدادات بقى فيه مؤشر حي بيوريك: بيزامن دلوقتي / آخر حفظ ناجح إمتى بالظبط / في مشكلة وبيعيد المحاولة.
- البيانات المحلية على جهازك تفضل زي ما هي دايمًا مهما حصل — أي فشل في المزامنة السحابية معناه بس إن النسخة السحابية اتأخرت شوية، مش إن فيه بيانات ضاعت.

## جديد: تبويب "السوشيال والموقع" — فيسبوك / انستقرام / تيك توك / إكس / لوكيشن
- تبويب جديد في الإعدادات اسمه **السوشيال والموقع** فيه الأربع منصات (فيسبوك، انستقرام، تيك توك، إكس) بالإضافة لرابط **موقعك على الخريطة (Google Maps)** — كل واحد بأيقونة مميزة.
- لكل رابط: تقدر **تغيّره، تعدّل اسمه، تخفيه من غير ما تمسحه (زرار "ظاهر/مخفي")، أو تمسحه نهائي**.
- تقدر **تضيف أكتر من رابط** لنفس المنصة (مثلاً صفحتين فيسبوك) أو منصة تانية غير الأربعة دول (تليجرام، سناب شات، أي رابط مخصص) عن طريق "إضافة رابط جديد".
- الروابط المفعّلة (اللي ليها رابط حقيقي ومظهورة) بتظهر تلقائيًا في فوتر الموقع: عمود "تواصل" بالاسم، وصف أيقونات دائرية تحته — ولو لسه محدش اتفعّل، العمود والصف بيختفوا تلقائيًا من غير أي مكان فاضي أو روابط وهمية.
- الملفات الجديدة: `src/lib/socialLinks.ts`، `src/components/SocialIcon.tsx`. الفوتر (`Footer.tsx`) والإعدادات (`SettingsPanel.tsx`) بقوا بيقروا من نفس المصدر.

## تعديل: شيل زرار/كلمة "ملاحظة" من كارت الطلب السريع
- الزرار الصغير اللي كان بيظهر جنب أزرار +/- في كارت الصنف (بيفتح خانة ملاحظة لكل صنف لوحده) اتشال بالكامل لتبسيط الواجهة.
- خانة "ملاحظة عامة على الطلب كله" في شاشة إتمام الطلب لسه موجودة زي ما هي، وهي كفاية لأي ملاحظة على الأوردر.

## جديد: "كل نصوص الموقع" (والألوان والخطوط وساعات العمل وطرق الدفع...) بقت تتزامن هي كمان
كان فيه فجوة مهمة: لما تفعّل Supabase أو Firebase، كان بيتزامن بس بيانات المنيو (الأصناف، الأقسام،
الإضافات، الطلبات، العملاء، الصور) — أما أي تعديل من تبويب **"كل نصوص الموقع"**، أو الألوان، أو الخط،
أو ساعات العمل، أو طرق الدفع، أو أرقام الواتساب... كان بيفضل في جهازك إنت بس، حتى لو السحابة شغالة
١٠٠٪، لأن الكود كان بيستثنيها من غير قصد. اتصلح دلوقتي، وبقت تتزامن بنفس الطريقة وبنفس السرعة (لحظي مع
Supabase/Firebase، كل ١٠ ثواني مع GitHub).

**اللي بيتزامن دلوقتي (بالإضافة لبيانات المنيو):**
كل نصوص الموقع، لون كل نص، الخط، ساعات العمل، قواعد الطلب، طرق الدفع وأرقامها، رسالة ما بعد الدفع،
أرقام الواتساب، الروابط المخصصة، إعدادات الباركود/الترابيزة، أعلام "سبق وشتغلنا في".

**اللي **مش** بيتزامن عمدًا (وده مقصود لحمايتك):**
- كلمة سر الإعدادات — مالهاش داعي تتخزن في جهاز أي زائر.
- بيانات الاتصال بالتخزين السحابي نفسه (رابط Supabase / مفاتيح Firebase / توكن GitHub) — عشان توكن
  GitHub تحديدًا بيدي صلاحية كتابة على المستودع، ومش آمن إنه ينتشر لجهاز كل زائر عادي.
- تفضيل اللغة، وبيانات أي زبون شخصية (الاسم/الموبايل اللي بيحفظه لنفسه) — دي خاصة بكل جهاز لوحده.

**متطلب واحد لو هتستخدم Supabase تحديدًا:** لازم تعمل جدول إضافي اسمه `settings` (بجانب الجداول التانية
اللي عندك أصلاً زي menuItems وcategories) بعمودين: `id` (نوع text، Primary Key) و`value` (نوع **jsonb**
مهم يبقى jsonb مش text). لو بتستخدم Firebase أو GitHub، مفيش أي إعداد إضافي مطلوب — هيتعمل لوحده تلقائي.

## إصلاح إضافي: بيانات متداخلة (نصوص فيها ألوان، قوائم) بقت تتزامن صح مع Firebase
الكود القديم اللي بيحوّل البيانات لصيغة Firestore كان بيحوّل أي حاجة "معقدة" (object جوه object، أو
array) لنص حرفي زي `[object Object]` بدل القيمة الحقيقية. اتصلح خالص دلوقتي (بقى بيدعم map/array بشكل
صحيح ومتداخل)، وده أصلاً كان ممكن يأثر على أي صنف في المنيو له بيانات متداخلة، مش بس على النصوص الجديدة.
